{
  "name": "claude-manager-skill",
  "version": "0.0.0",
  "private": true,
  "description": "The /manager Claude Code skill, plus tests for its artifact consistency and Review-Loop guard logic",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}